import { Story, Meta } from '@storybook/blocks';
import * as LayersControlStories from './LayersControl.stories';
 
<Meta of={LayersControlStories} />
 
# {'<LayersControl />'}

Show and hide layers or layer groups

### How it works
1. List all layer(group)s
2. When a layer(group) is clicked, show and hide the layer(group).

<Story of={LayersControlStories.Primary} />

### Usage
```typescript
```