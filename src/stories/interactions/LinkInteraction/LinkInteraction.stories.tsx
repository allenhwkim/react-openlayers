import {Map, LinkInteraction} from '../../../lib';

export default {
  title: 'Interactions/LinkInteraction',
}

export const Primary = {
  render: (props) => {

    return (
      <Map>
        <LinkInteraction />
      </Map>
    );
  }
}